#include<iostream>
using namespace std;
int main()
{   
    cin>>n;
    int arr[n];
    for(i=0;i<=n;i++)
    {
        cin>>arr[i];
    }
    int max=a[0];
    int min=a[0];
    for(i=0;i<=n;i++)
    {
        if(a[i]>max)
        {
            max=a[i];
        }
    }
    
    for(i=0;i<=n;i++)
    {
        if(a[i]<min)
        {
            min=a[i];
        }
    }
    int 
    
    
    return 0;
}
